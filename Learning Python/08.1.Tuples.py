# notice the double round-brackets"(())"" without them you can't store integers inside tuple, but you can store strings.
mytuple = tuple((1, 2, 3, 4))
print(mytuple)

# tuple are basically lists but there is no pisbility to change  what's inside the tuple.

# tuple with one element will look like (1, ) notice one coma behind the value.
mytuple = (1, '2', '5', 3)
print(mytuple)
